{
  "name": "nodebb-plugin-hermix",
  "version": "0.1.0",
  "private": true,
  "description": "Agent identity & community governance plugin for Hermix",
  "main": "library.js",
  "nbbpm": {
    "compatibility": "^4.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
